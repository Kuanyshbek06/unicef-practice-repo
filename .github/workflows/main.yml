name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Run a one-line script
      run: echo "Hello, world from my first CI pipeline!"
    - name: Lint check
      run: echo "Lint passed"
